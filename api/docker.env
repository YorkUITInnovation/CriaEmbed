# MySQL:
MYSQL_PASSWORD=Password for MySQL database
MYSQL_PORT=MySQL port (typically 3306)
MYSQL_HOST=MySQL host address
MYSQL_USERNAME=MySQL username
MYSQL_DATABASE=MySQL database name

# Redis:
REDIS_PORT - Redis port (typically 6379)
REDIS_HOST - Redis host address
REDIS_USERNAME - Redis username
REDIS_PASSWORD=Redis password

# Rate Limiting
RATE_LIMIT_EMBED_MINUTE_MAX - Max embed requests per minute
RATE_LIMIT_EMBED_HOUR_MAX - Max embed requests per hour
RATE_LIMIT_EMBED_DAY_MAX - Max embed requests per day
RATE_LIMIT_CHAT_MINUTE_MAX - Max chat requests per minute
RATE_LIMIT_CHAT_HOUR_MAX - Max chat requests per hour
RATE_LIMIT_CHAT_DAY_MAX - Max chat requests per day

# Application URLs & Tokens
WEB_APP_URL - URL of your web application
CRIA_SERVER_URL - URL of the Cria server
CRIA_BOT_SERVER_URL - URL of the Cria bot server
CRIA_BOT_SERVER_TOKEN - Authentication token for bot server
CRIA_SERVER_TOKEN - Authentication token for Cria server
THIS_APP_URL - URL of this embed application

# Azure Speech Service
AZURE_SPEECH_API_URL - Azure Speech Service API endpoint
AZURE_SPEECH_API_KEY - Azure Speech Service API key

# General Settings
APP_MODE - Either "PRODUCTION" or "TESTING"
DEFAULT_BOT_GREETING - Default greeting message for the bot
ASSETS_FOLDER_PATH - Path to assets folder (defaults to "./src/assets/")

#Optional Variables
PORT - Server port (defaults to 3003 if not set)
DEBUG_ENABLED - Set to "true" to enable debug mode
ENV_PATH - Custom path to .env file (defaults to "./.env")